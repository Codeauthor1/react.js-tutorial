/**
 * # Creating and nesting components
 *  React apps are made out of a components.
 * A component is a piece of the UI (user interface)
 * that has its own login and appearance. A component 
 * can be as small as a button, or as large as an entire
 * page
 * 
 * React component are javaScript function that return
 * markup:  
* */

export function MyButton() {
    return (
        <button>I'm a button</button>
    )
}